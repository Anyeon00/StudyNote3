1031 운영체제 유닉스파일시스템(보강)

여태 수업과 관련된 부분

파일시스템 - inode

file table
- current file offset(현재읽던 위치)

읽기-쓰기 포인터(파일 포인터) : 문맥교환시 파일주소 위치를 저장하는 포인터

파일공유
PCB 

OS안에서 읽는 함수는 read() 1개
dup()
: 원래 두개의 file table이 같은 파일을 가리켜도 각각 다른 프로세스이므로 현재 정보가 다르게 관리되는데,
같이 관리하게 하는 함수
(테이블에서 사용하지 않는거의 가장 앞에있는거)

fork() 현재 읽던 위치(offset)도 복사됨
